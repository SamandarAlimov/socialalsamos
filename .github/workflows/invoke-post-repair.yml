name: One-shot Post Visibility Repair

on:
  push:
    branches: [main]
    paths:
      - "api/repair-post-visibility.ts"
      - ".github/workflows/invoke-post-repair.yml"

jobs:
  repair:
    runs-on: ubuntu-latest
    timeout-minutes: 5
    steps:
      - name: Wait for production deploy and repair
        shell: bash
        run: |
          set -euo pipefail
          sleep 25
          TMP="$(mktemp)"
          STATUS="$(curl --silent --show-error --max-time 45 -o "$TMP" -w '%{http_code}' https://www.alsamos.com/api/repair-post-visibility)"
          echo "HTTP $STATUS"
          cat "$TMP"
          echo
          test "$STATUS" = "200"
