name: Apply video watch controls follow-up

on:
  push:
    branches:
      - fix/video-watch-autoplay-zoom-pan

permissions:
  contents: write

jobs:
  patch:
    if: ${{ !contains(github.event.head_commit.message, '[watch-controls-followup-applied]') }}
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
        with:
          ref: fix/video-watch-autoplay-zoom-pan
          fetch-depth: 0

      - uses: actions/setup-node@v4
        with:
          node-version: 20
          cache: npm

      - name: Apply follow-up player controls patch
        run: python3 .github/scripts/apply_video_watch_controls_followup.py

      - name: Install dependencies
        run: npm ci

      - name: Video interaction regression
        run: npx vitest run src/hooks/usePinchZoom.test.tsx src/hooks/useVideoSurfaceTap.test.tsx

      - name: Typecheck
        run: npm run typecheck:create

      - name: Commit patch and remove temporary files
        run: |
          rm .github/workflows/apply-video-watch-controls-followup.yml
          rm .github/scripts/apply_video_watch_controls_followup.py
          git config user.name "github-actions[bot]"
          git config user.email "41898282+github-actions[bot]@users.noreply.github.com"
          git add -A
          git commit -m "fix: complete Watch controller parity [watch-controls-followup-applied]"
          git push origin HEAD:fix/video-watch-autoplay-zoom-pan
