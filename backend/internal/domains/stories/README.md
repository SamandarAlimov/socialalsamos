# stories

Structure only. Planned capabilities: story, archive, viewers.

Implement the domain rules, actor authorization, transaction boundaries and failure cases from [backend.md](../../../../backend.md). Empty test files do not constitute verification. SQL belongs in `db/queries/stories/`; API schemas and examples belong in `contracts/`.
