# comments

Structure only. Planned capabilities: comment, replies.

Implement the domain rules, actor authorization, transaction boundaries and failure cases from [backend.md](../../../../backend.md). Empty test files do not constitute verification. SQL belongs in `db/queries/comments/`; API schemas and examples belong in `contracts/`.
