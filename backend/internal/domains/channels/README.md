# channels

Structure only. Planned capabilities: channel, roles, invitations, topics.

Implement the domain rules, actor authorization, transaction boundaries and failure cases from [backend.md](../../../../backend.md). Empty test files do not constitute verification. SQL belongs in `db/queries/channels/`; API schemas and examples belong in `contracts/`.
