# profiles

Structure only. Planned capabilities: profile, username, avatar.

Implement the domain rules, actor authorization, transaction boundaries and failure cases from [backend.md](../../../../backend.md). Empty test files do not constitute verification. SQL belongs in `db/queries/profiles/`; API schemas and examples belong in `contracts/`.
