# privacy

Structure only. Planned capabilities: audience, consent, discoverability.

Implement the domain rules, actor authorization, transaction boundaries and failure cases from [backend.md](../../../../backend.md). Empty test files do not constitute verification. SQL belongs in `db/queries/privacy/`; API schemas and examples belong in `contracts/`.
