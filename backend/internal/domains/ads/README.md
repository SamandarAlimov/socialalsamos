# ads

Structure only. Planned capabilities: campaigns, delivery, budgets, experiments, integrity, analytics.

Implement the domain rules, actor authorization, transaction boundaries and failure cases from [backend.md](../../../../backend.md). Empty test files do not constitute verification. SQL belongs in `db/queries/ads/`; API schemas and examples belong in `contracts/`.
