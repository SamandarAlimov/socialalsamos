# calls

Structure only. Planned capabilities: lifecycle, invitations, participants, tokens, webhooks, timeouts.

Implement the domain rules, actor authorization, transaction boundaries and failure cases from [backend.md](../../../../backend.md). Empty test files do not constitute verification. SQL belongs in `db/queries/calls/`; API schemas and examples belong in `contracts/`.
