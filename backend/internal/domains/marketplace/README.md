# marketplace

Structure only. Planned capabilities: catalog, sellers, cart, checkout, orders, inventory, reviews.

Implement the domain rules, actor authorization, transaction boundaries and failure cases from [backend.md](../../../../backend.md). Empty test files do not constitute verification. SQL belongs in `db/queries/marketplace/`; API schemas and examples belong in `contracts/`.
