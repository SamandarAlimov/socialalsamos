# exports

Structure only. Planned capabilities: requests, archive, deletion, retention.

Implement the domain rules, actor authorization, transaction boundaries and failure cases from [backend.md](../../../../backend.md). Empty test files do not constitute verification. SQL belongs in `db/queries/exports/`; API schemas and examples belong in `contracts/`.
