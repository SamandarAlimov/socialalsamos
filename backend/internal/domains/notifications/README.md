# notifications

Structure only. Planned capabilities: inbox, preferences, delivery, push_tokens.

Implement the domain rules, actor authorization, transaction boundaries and failure cases from [backend.md](../../../../backend.md). Empty test files do not constitute verification. SQL belongs in `db/queries/notifications/`; API schemas and examples belong in `contracts/`.
