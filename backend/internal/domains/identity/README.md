# identity

Structure only. Planned capabilities: accounts, sessions, devices, mfa, recovery, account_slots.

Implement the domain rules, actor authorization, transaction boundaries and failure cases from [backend.md](../../../../backend.md). Empty test files do not constitute verification. SQL belongs in `db/queries/identity/`; API schemas and examples belong in `contracts/`.
