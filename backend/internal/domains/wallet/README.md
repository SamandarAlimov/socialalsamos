# wallet

Structure only. Planned capabilities: accounts, ledger, transfers, balances, reconciliation.

Implement the domain rules, actor authorization, transaction boundaries and failure cases from [backend.md](../../../../backend.md). Empty test files do not constitute verification. SQL belongs in `db/queries/wallet/`; API schemas and examples belong in `contracts/`.
