# moderation

Structure only. Planned capabilities: reports, cases, decisions, appeals.

Implement the domain rules, actor authorization, transaction boundaries and failure cases from [backend.md](../../../../backend.md). Empty test files do not constitute verification. SQL belongs in `db/queries/moderation/`; API schemas and examples belong in `contracts/`.
