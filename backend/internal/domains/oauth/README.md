# oauth

Structure only. Planned capabilities: clients, authorization, tokens, consent, discovery, revocation.

Implement the domain rules, actor authorization, transaction boundaries and failure cases from [backend.md](../../../../backend.md). Empty test files do not constitute verification. SQL belongs in `db/queries/oauth/`; API schemas and examples belong in `contracts/`.
