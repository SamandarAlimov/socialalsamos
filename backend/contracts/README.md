# Contracts

Reserved API, event and fixture files are empty until baseline reconciliation. Do not treat empty JSON/YAML as validated contracts. Populate parity.csv from static and runtime inventory before implementation acceptance.
