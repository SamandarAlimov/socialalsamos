# CI plan

Reserved CI configuration is kept here rather than in the active repository workflow directory. Activate only after the Go module and executable tests exist. No existing workflow is changed by this scaffold.
