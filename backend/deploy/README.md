# Deployment

No runnable deployment exists at scaffold stage. Compose is planned for isolated development. Staging/production files reserve configuration categories without selecting Kubernetes or provisioning any infrastructure. Containers, secrets, network rules and restore procedures require implementation and verification.
